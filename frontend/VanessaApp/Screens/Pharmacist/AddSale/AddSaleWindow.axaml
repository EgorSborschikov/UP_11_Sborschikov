<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VanessaApp.ViewModels"
        mc:Ignorable="d"
        d:DesignWidth="600" d:DesignHeight="450"
        Title="Продажа товара">
    
    <!--<Grid RowDefinitions="Auto,Auto,*,Auto,Auto" 
          ColumnDefinitions="*,*"
          Margin="10">
        
         
        <TextBlock Text="Выберите клиента:" Margin="5"/>
        <ComboBox Grid.Row="0" Grid.Column="0"
                  ItemsSource="{Binding Clients}"
                  SelectedItem="{Binding SelectedClient}"
                  DisplayMemberBinding="{Binding FullName}"
                  Margin="5" Width="300" HorizontalAlignment="Left"/>
                  
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Выберите товары:" Margin="5"/>
        <DataGrid Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2"
                  ItemsSource="{Binding Products}"
                  AutoGenerateColumns="False"
                  Margin="5" Height="250">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Header="Выбор" Binding="{Binding IsSelected}"/>
                <DataGridTextColumn Header="Название" Binding="{Binding Product.Name}" IsReadOnly="True"/>
                <DataGridTextColumn Header="Цена" Binding="{Binding Product.Price}" IsReadOnly="True"/>
                <DataGridTemplateColumn Header="Количество">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <NumericUpDown Minimum="1" Value="{Binding Quantity}" Width="80"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>


        <TextBlock Grid.Row="3" Grid.Column="0" 
                 Text="{Binding TotalPrice, StringFormat='Итого: {0} руб.'}"
                 Margin="5" FontWeight="Bold"/>


        <Button Grid.Row="4" Grid.Column="1" 
              Content="Оплатить" 
              Click="PayButton_Click"
              Margin="5" Width="120" HorizontalAlignment="Right"/>
    </Grid> -->
</Window> 